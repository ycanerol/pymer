{
    // Do not modify this file. This is the default configuration.
    // To adjust the settings, copy and modify them in a file named .pymer
    // in your home directory.


    // The base directory containing sub directories for all experiments.
    // The 'null' forces the user to create a custom configuration with a
    // valid path to the local experiment directory.
    "root_experiment_dir": null,

    // List of prefixes tried for matching experiment names.
    "experiment_prefixes": [],

    // List of file names (excluding file extension) for spike sheets.
    "spike_sorting_filenames":
    [
        "spike_sorting"
    ],

    // Search and replace pairs for paths in stimulus files, e.g.
    // "C:": "/"
    "stimuli_path_replace": {}
}
